var React     = require('react'),
    component = require('omniscient'),
    immstruct = require('immstruct');

var d = React.DOM;

var App = component('Screen', function (props) {
  return 
});

module.exports = Login;
